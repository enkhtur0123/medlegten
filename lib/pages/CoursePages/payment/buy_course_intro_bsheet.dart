import 'package:flutter/material.dart';

class BuyCourseIntroWidget extends StatelessWidget {
  const BuyCourseIntroWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
